apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: velure-publish-order-deny-all
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app: velure-publish-order
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress: []

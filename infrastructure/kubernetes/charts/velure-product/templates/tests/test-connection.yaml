apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "velure-product.fullname" . }}-test-connection"
  labels:
    {{- include "velure-product.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "velure-product.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

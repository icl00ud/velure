apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: velure-process-order-deny-all
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app: velure-process-order
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress: []

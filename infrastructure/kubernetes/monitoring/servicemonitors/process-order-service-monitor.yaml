apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: velure-process-order
  namespace: default
  labels:
    app: velure-process-order
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: velure-process-order
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
  namespaceSelector:
    matchNames:
      - order

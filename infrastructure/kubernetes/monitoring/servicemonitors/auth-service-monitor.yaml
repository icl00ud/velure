apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: velure-auth
  namespace: default
  labels:
    app: velure-auth
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: velure-auth
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
  namespaceSelector:
    matchNames:
      - authentication
      - default

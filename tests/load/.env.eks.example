# Velure Load Testing - AWS EKS Configuration
# Copy this file to .env.eks and adjust with your ALB URL

# Get ALB URL with:
# kubectl get ingress velure-ui -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'

ALB_URL=your-alb-url.us-east-1.elb.amazonaws.com

# Service URLs (through ALB)
BASE_URL=http://${ALB_URL}
AUTH_URL=http://${ALB_URL}/api/auth
PRODUCT_URL=http://${ALB_URL}/api/product
ORDER_URL=http://${ALB_URL}/api/order
UI_URL=http://${ALB_URL}

# Test duration configuration
WARMUP_DURATION=60s
TEST_DURATION=30s

# Usage:
# source .env.eks
# k6 run -e AUTH_URL=$AUTH_URL -e WARMUP_DURATION=$WARMUP_DURATION auth-service-test.js

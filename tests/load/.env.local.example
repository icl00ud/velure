# Velure Load Testing - Local Kubernetes Configuration
# Copy this file to .env.local and adjust as needed

# For local Kubernetes (minikube/kind/Docker Desktop)
# Using Ingress or port-forward

BASE_URL=https://velure.local

# Service URLs (if using Ingress with routing)
AUTH_URL=https://velure.local/api/auth
PRODUCT_URL=https://velure.local/api/product
ORDER_URL=https://velure.local/api/order
UI_URL=https://velure.local

# Or if using port-forward:
# AUTH_URL=http://localhost:3020
# PRODUCT_URL=http://localhost:3010
# ORDER_URL=http://localhost:8080
# UI_URL=http://localhost:8081

# Test duration configuration
WARMUP_DURATION=30s
TEST_DURATION=15s

# Usage:
# source .env.local
# k6 run -e AUTH_URL=$AUTH_URL -e WARMUP_DURATION=$WARMUP_DURATION auth-service-test.js

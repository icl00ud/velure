# Local Development Load Test Configuration
# Copy this file to .env.local and update with your actual values

# Base URL for local development
# Default local setup uses velure.local with Caddy reverse proxy
BASE_URL=https://velure.local

# Optional: Override test duration and load for local testing
# For local testing, you might want lighter load:
# K6_STAGES='[{"duration":"30s","target":5},{"duration":"1m","target":10},{"duration":"30s","target":0}]'
# K6_STAGES=

# Optional: Number of virtual users (overrides stages if set)
# K6_VUS=5

# Optional: Test duration (overrides stages if set)
# K6_DURATION=2m

# Optional: K6 output options
# K6_OUT=json=results.json
